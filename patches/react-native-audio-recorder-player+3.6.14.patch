diff --git a/node_modules/react-native-audio-recorder-player/android/src/main/java/com/dooboolab.audiorecorderplayer/RNAudioRecorderPlayerModule.kt b/node_modules/react-native-audio-recorder-player/android/src/main/java/com/dooboolab.audiorecorderplayer/RNAudioRecorderPlayerModule.kt
index 4570fc2..880f157 100644
--- a/node_modules/react-native-audio-recorder-player/android/src/main/java/com/dooboolab.audiorecorderplayer/RNAudioRecorderPlayerModule.kt
+++ b/node_modules/react-native-audio-recorder-player/android/src/main/java/com/dooboolab.audiorecorderplayer/RNAudioRecorderPlayerModule.kt
@@ -2,6 +2,8 @@ package com.dooboolab.audiorecorderplayer
 
 import android.Manifest
 import android.content.pm.PackageManager
+import android.media.AudioAttributes
+import android.media.AudioManager
 import android.media.MediaPlayer
 import android.media.MediaRecorder
 import android.net.Uri
@@ -242,20 +244,33 @@ class RNAudioRecorderPlayerModule(private val reactContext: ReactApplicationCont
         }
 
         try {
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.LOLLIPOP) {
+                mediaPlayer!!.setAudioAttributes(
+                    AudioAttributes.Builder()
+                        .setUsage(AudioAttributes.USAGE_MEDIA)
+                        .setContentType(AudioAttributes.CONTENT_TYPE_SPEECH)
+                        .build()
+                )
+            } else {
+                @Suppress("DEPRECATION")
+                mediaPlayer!!.setAudioStreamType(AudioManager.STREAM_MUSIC)
+            }
+
+            val appContext = reactContext.applicationContext
             if ((path == "DEFAULT")) {
                 mediaPlayer!!.setDataSource("${reactContext.cacheDir}/$defaultFileName")
-            } else {
-                if (httpHeaders != null) {
-                    val headers: MutableMap<String, String?> = HashMap<String, String?>()
-                    val iterator = httpHeaders.keySetIterator()
-                    while (iterator.hasNextKey()) {
-                        val key = iterator.nextKey()
-                        headers.put(key, httpHeaders.getString(key))
-                    }
-                    mediaPlayer!!.setDataSource(currentActivity!!.applicationContext, Uri.parse(path), headers)
-                } else {
-                    mediaPlayer!!.setDataSource(path)
+            } else if (httpHeaders != null) {
+                val headers: MutableMap<String, String?> = HashMap<String, String?>()
+                val iterator = httpHeaders.keySetIterator()
+                while (iterator.hasNextKey()) {
+                    val key = iterator.nextKey()
+                    headers.put(key, httpHeaders.getString(key))
                 }
+                mediaPlayer!!.setDataSource(appContext, Uri.parse(path), headers)
+            } else if (path.startsWith("http://") || path.startsWith("https://") || path.startsWith("file://")) {
+                mediaPlayer!!.setDataSource(appContext, Uri.parse(path))
+            } else {
+                mediaPlayer!!.setDataSource(path)
             }
 
             mediaPlayer!!.setOnPreparedListener { mp ->
@@ -309,11 +324,16 @@ class RNAudioRecorderPlayerModule(private val reactContext: ReactApplicationCont
             }
 
             mediaPlayer!!.prepare()
-        } catch (e: IOException) {
-            Log.e(tag, "startPlay() io exception")
+        } catch (e: Exception) {
+            Log.e(tag, "startPlay() exception: ${e.message}")
+            try {
+                mediaPlayer?.reset()
+                mediaPlayer?.release()
+            } catch (releaseError: Exception) {
+                Log.e(tag, "startPlay() release exception: ${releaseError.message}")
+            }
+            mediaPlayer = null
             promise.reject("startPlay", e.message)
-        } catch (e: NullPointerException) {
-            Log.e(tag, "startPlay() null exception")
         }
     }
 
diff --git a/node_modules/react-native-audio-recorder-player/ios/RNAudioRecorderPlayer.swift b/node_modules/react-native-audio-recorder-player/ios/RNAudioRecorderPlayer.swift
index 344ddd4..a6da402 100644
--- a/node_modules/react-native-audio-recorder-player/ios/RNAudioRecorderPlayer.swift
+++ b/node_modules/react-native-audio-recorder-player/ios/RNAudioRecorderPlayer.swift
@@ -243,17 +243,21 @@ class RNAudioRecorderPlayer: RCTEventEmitter, AVAudioRecorderDelegate {
         }
 
         func startRecording() {
-            let settings = [
+            var settings: [String: Any] = [
                 AVSampleRateKey: sampleRate!,
                 AVFormatIDKey: avFormat,
                 AVNumberOfChannelsKey: numberOfChannel!,
-                AVEncoderAudioQualityKey: audioQuality!,
-                AVLinearPCMBitDepthKey: avLPCMBitDepth ?? AVLinearPCMBitDepthKey.count,
-                AVLinearPCMIsBigEndianKey: avLPCMIsBigEndian ?? true,
-                AVLinearPCMIsFloatKey: avLPCMIsFloatKey ?? false,
-                AVLinearPCMIsNonInterleaved: avLPCMIsNonInterleaved ?? false,
-                 AVEncoderBitRateKey: bitRate!
-            ] as [String : Any]
+            ]
+
+            if avFormat == kAudioFormatLinearPCM {
+                settings[AVLinearPCMBitDepthKey] = avLPCMBitDepth ?? 16
+                settings[AVLinearPCMIsBigEndianKey] = avLPCMIsBigEndian ?? false
+                settings[AVLinearPCMIsFloatKey] = avLPCMIsFloatKey ?? false
+                settings[AVLinearPCMIsNonInterleaved] = avLPCMIsNonInterleaved ?? false
+            } else {
+                settings[AVEncoderAudioQualityKey] = audioQuality!
+                settings[AVEncoderBitRateKey] = bitRate!
+            }
 
             do {
                 audioRecorder = try AVAudioRecorder(url: audioFileURL!, settings: settings)
